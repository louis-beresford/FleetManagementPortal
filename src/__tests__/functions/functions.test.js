import {
  toggleBikeForm,
  toggleUserForm,
  toggleBikeEdit,
  toggleUserEdit,
  getRiders,
} from "../../pages/admin";

test("toogleBikeForm", () => {
  toggleBikeEdit;
  toggleUserEdit;
  toggleUserForm;
  toggleBikeForm;
  getRiders;
});
